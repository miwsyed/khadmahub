# KhadmaHub

Monorepo for the KhadmaHub project.

## Structure

- `client/` — React + Vite frontend
- `server/` — ASP.NET Core backend

## Getting started

1. Install frontend dependencies in `client/`
2. Restore and run the backend in `server/`
3. Start the frontend and API as needed for local development
